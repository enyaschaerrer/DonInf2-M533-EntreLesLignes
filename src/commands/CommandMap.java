package commands;
import zone.Zone;

public class CommandMap extends Command {
    public CommandMap() {
        super("map", "It display the map.");
    }

    @Override
    protected String doExecute(Joueur player, Zone zone) {
        
    }
}